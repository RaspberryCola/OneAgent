{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for OneAgent",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
